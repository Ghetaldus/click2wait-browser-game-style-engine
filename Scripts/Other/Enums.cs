// (c) www.click2wait.net

using System;
using System.Text;
using UnityEngine;
using click2wait;

namespace click2wait {
	
	public enum NetworkState 	{Offline,Game}
	public enum SortOrder 		{None, Priority}
	public enum DisplayType 	{Horizontal, Vertical, Grid}
	public enum ObjectType 		{None,Achievement,Buff,Building,Currency,Enhancement,Equipment,Item,Mission,Skill,Spell,Technology,Unit}
	public enum ActionType 		{None,List,Acquire,Unacquire,Upgrade,View}
	
}